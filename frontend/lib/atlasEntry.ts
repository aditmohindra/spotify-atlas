/** sessionStorage key — set after the user clicks "Enter Atlas". */
export const ATLAS_ENTERED_KEY = "atlas-entered";

/** window event — fires on Enter Atlas click so the map can start its entrance. */
export const ATLAS_ENTER_EVENT = "atlas-enter";
